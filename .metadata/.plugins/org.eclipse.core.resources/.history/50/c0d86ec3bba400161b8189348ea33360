package Helper;

import java.sql.Time;
import java.time.LocalTime;

public class TimeHandler {
	TimeHandler(){}
	public static String whatSession(LocalTime time){
		LocalTime AMfirst = Time.valueOf("11:00:00").toLocalTime();
		LocalTime AMsecond = Time.valueOf("15:00:00").toLocalTime();
		LocalTime PMfirst = Time.valueOf("11:00:00").toLocalTime();
		LocalTime PMsecond = Time.valueOf("11:00:00").toLocalTime();
		if time.compareTo(other)
	}
}
